[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
pythonpath = .
markers =
    unit: fast isolated unit tests
    integration: component tests with mocks / local sockets
    e2e: optional live checks (need running proxy / network)
filterwarnings =
    ignore::DeprecationWarning
